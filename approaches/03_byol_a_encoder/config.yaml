approach: "03_byol_a_encoder"

byol_a:
  batch_size: 64
  num_epochs: 50
  learning_rate: 3.0e-4
  ema_decay: 0.99
  mixup_alpha: 0.4

cond:
  source: "byol_a"
  dim: 512

paths:
  byol_checkpoint: "tmodels/03_byol_a_encoder/byol_a_encoder.pth"
  byol_features: "datasets/byol_features"
  mel_cache: "datasets/byol_mel_cache"
